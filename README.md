# Xevos App - Daniel Komínek

# Instalace Laravelu

 - composer install
 - php artisan migrate (vytvori a namigruje sqlite db)
 - php artisan serve --host=localhost (vygeneruje url, kde běží server)

# Instalace  ReactJS

 - npm install
 - npm run watch

# Prohlížeč

 - otevřít vygenerovanou url